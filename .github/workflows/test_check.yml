name: Check test 1

on:
  check_suite:
    types: [completed]

jobs:
  other-job:
    runs-on: ubuntu-20.04
    steps:
      - name: test echo
        shell: bash
        run: echo "Is running"

      - name: data
        run: echo "${{toJson(github)}}"
        